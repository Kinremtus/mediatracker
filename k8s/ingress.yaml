apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: mediatracker
  namespace: mediatracker
spec:
  entryPoints:
    - web
  routes:
    - kind: Rule
      match: Host(`mediatracker.web-socket-test-bench.site`)
      services:
        - name: app-service
          port: 80